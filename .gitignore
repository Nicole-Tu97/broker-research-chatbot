# secrets — never commit
.env

# python
__pycache__/
*.pyc
.venv/

# macOS
.DS_Store

# runtime page assets (re-rendered locally via make render)
page_assets/

# bench outputs (rendered pages, model outputs)
bench/out/
bench/out_tiers/

# index fixture — embeds transcribed licensed content; ships in the private
# submission package, not in the public repo
fixtures/

# MS Office lock/temp files
~$*

# benchmark answer key — verbatim content of 20 licensed pages; ships in the
# private submission package, not the public repo (same policy as fixtures/)
bench/ground_truth.md

# local demo material: sample transcriptions (licensed page content, not published)
transcription-samples/
