# 复制为 .env 并填入你的 key：cp .env.example .env
OPENAI_API_KEY=sk-...

# 以下均有默认值，通常无需改动（见 config/settings.py）
OPENAI_VISION_MODEL=gpt-5.6-sol
OPENAI_EMBED_MODEL=text-embedding-3-large
OPENAI_IMAGE_DETAIL=original
# DB_HOST=127.0.0.1  # docker compose 内为 db
